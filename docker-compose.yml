version: '3.8'

services:
  app:
    build: .
    container_name: kodlab-vulnerable
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    volumes:
      # Сохраняем базу данных между перезапусками
      - ./data:/app/data
    restart: unless-stopped
