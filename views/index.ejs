<!DOCTYPE html>
<html lang="ru">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>КодЛаб — Разработка на заказ</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }

    .gradient-bg {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }

    .card-shadow {
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>

<body class="bg-gray-50">
  <!-- Навигация -->
  <nav class="bg-white shadow-sm sticky top-0 z-50">
    <div class="max-w-6xl mx-auto px-4 py-4">
      <div class="flex justify-between items-center">
        <a href="/" class="text-2xl font-bold text-purple-600">{ КодЛаб }</a>
        <div class="hidden md:flex space-x-8">
          <a href="#services" class="text-gray-600 hover:text-purple-600 transition">Услуги</a>
          <a href="#reviews" class="text-gray-600 hover:text-purple-600 transition">Отзывы</a>
          <a href="#contact" class="text-gray-600 hover:text-purple-600 transition">Контакты</a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Hero секция -->
  <section class="gradient-bg text-white py-24">
    <div class="max-w-6xl mx-auto px-4 text-center">
      <h1 class="text-4xl md:text-6xl font-bold mb-6">
        Разрабатываем цифровые<br>решения для бизнеса
      </h1>
      <p class="text-xl text-purple-100 mb-8 max-w-2xl mx-auto">
        Сайты, веб-приложения, мобильные приложения и CRM-системы под ключ.
        Работаем с 2019 года.
      </p>
      <a href="#contact"
        class="inline-block bg-white text-purple-600 font-semibold px-8 py-4 rounded-lg hover:bg-purple-50 transition shadow-lg">
        Обсудить проект
      </a>
    </div>
  </section>

  <!-- Услуги -->
  <section id="services" class="py-20">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12">Наши услуги</h2>
      <div class="grid md:grid-cols-3 gap-8">
        <div class="bg-white p-8 rounded-2xl card-shadow">
          <div class="w-14 h-14 bg-purple-100 rounded-xl flex items-center justify-center mb-6">
            <svg class="w-7 h-7 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M9.75 17L9 20l-1 1h8l-1-1-.75-3M3 13h18M5 17h14a2 2 0 002-2V5a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z">
              </path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-3">Веб-сайты</h3>
          <p class="text-gray-600">Корпоративные сайты, лендинги, интернет-магазины с современным дизайном и адаптивной
            версткой.</p>
        </div>
        <div class="bg-white p-8 rounded-2xl card-shadow">
          <div class="w-14 h-14 bg-green-100 rounded-xl flex items-center justify-center mb-6">
            <svg class="w-7 h-7 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-3">Мобильные приложения</h3>
          <p class="text-gray-600">Нативные и кросс-платформенные приложения для iOS и Android с публикацией в сторах.
          </p>
        </div>
        <div class="bg-white p-8 rounded-2xl card-shadow">
          <div class="w-14 h-14 bg-blue-100 rounded-xl flex items-center justify-center mb-6">
            <svg class="w-7 h-7 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10">
              </path>
            </svg>
          </div>
          <h3 class="text-xl font-semibold mb-3">CRM-системы</h3>
          <p class="text-gray-600">Автоматизация бизнес-процессов, учет клиентов, интеграции с 1С, телефонией и почтой.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Отзывы -->
  <section id="reviews" class="py-20 bg-white">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12">Отзывы клиентов</h2>

      <!-- Список отзывов -->
      <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 mb-12">
        <% reviews.forEach(function(review) { %>
          <div class="bg-gray-50 p-6 rounded-xl">
            <div class="flex items-center mb-4">
              <div
                class="w-10 h-10 bg-purple-200 rounded-full flex items-center justify-center text-purple-700 font-semibold">
                <%= review.name.charAt(0).toUpperCase() %>
              </div>
              <!-- УЯЗВИМОСТЬ XSS: имя выводится без экранирования -->
              <span class="ml-3 font-medium"><%- review.name %></span>
            </div>
            <!-- УЯЗВИМОСТЬ XSS: текст выводится без экранирования -->
            <p class="text-gray-600"><%- review.text %></p>
          </div>
          <% }); %>
      </div>

      <!-- Форма добавления отзыва -->
      <div class="max-w-xl mx-auto">
        <h3 class="text-xl font-semibold text-center mb-6">Оставить отзыв</h3>
        <form action="/review" method="POST" class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Ваше имя</label>
            <input type="text" name="name" required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none transition"
              placeholder="Иван Иванов">
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">Ваш отзыв</label>
            <textarea name="text" rows="3" required
              class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none transition resize-none"
              placeholder="Расскажите о вашем опыте работы с нами..."></textarea>
          </div>
          <button type="submit"
            class="w-full bg-purple-600 text-white font-semibold py-3 rounded-lg hover:bg-purple-700 transition">
            Отправить отзыв
          </button>
        </form>
      </div>
    </div>
  </section>

  <!-- Контакты -->
  <section id="contact" class="py-20">
    <div class="max-w-6xl mx-auto px-4">
      <h2 class="text-3xl font-bold text-center mb-12">Связаться с нами</h2>

      <% if (typeof success !=='undefined' || (typeof query !=='undefined' && query.success)) { %>
        <div class="max-w-xl mx-auto mb-8 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg">
          Спасибо! Ваша заявка отправлена. Мы свяжемся с вами в ближайшее время.
        </div>
        <% } %>

          <div class="max-w-xl mx-auto">
            <form action="/contact" method="POST" class="bg-white p-8 rounded-2xl card-shadow space-y-6">
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                <input type="email" name="email" required
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none transition"
                  placeholder="your@email.com">
              </div>
              <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">Сообщение</label>
                <textarea name="message" rows="4" required
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-purple-500 focus:border-transparent outline-none transition resize-none"
                  placeholder="Опишите ваш проект или задайте вопрос..."></textarea>
              </div>
              <button type="submit"
                class="w-full bg-purple-600 text-white font-semibold py-4 rounded-lg hover:bg-purple-700 transition">
                Отправить заявку
              </button>
            </form>
          </div>
    </div>
  </section>

  <!-- Футер -->
  <footer class="bg-gray-900 text-gray-400 py-12">
    <div class="max-w-6xl mx-auto px-4 text-center">
      <div class="text-2xl font-bold text-white mb-4">{ КодЛаб }</div>
      <p class="mb-4">Разработка цифровых решений для бизнеса</p>
      <p class="text-sm">© 2026 КодЛаб. Все права защищены.</p>
    </div>
  </footer>

  <script>
    // Проверка success параметра
    const urlParams = new URLSearchParams(window.location.search)
    if (urlParams.get('success') === '1') {
      // Показываем уведомление
      const section = document.getElementById('contact')
      const alert = document.createElement('div')
      alert.className = 'max-w-xl mx-auto mb-8 bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg text-center'
      alert.textContent = 'Спасибо! Ваша заявка отправлена. Мы свяжемся с вами в ближайшее время.'
      section.querySelector('.max-w-6xl').insertBefore(alert, section.querySelector('.max-w-xl'))
    }
  </script>
</body>

</html>